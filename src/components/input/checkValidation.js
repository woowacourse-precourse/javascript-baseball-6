import { getRandomNumber } from '@components/answer';

// 유효한 입력인지 검사합니다.
const checkValidation = (input) => {
  const target = getRandomNumber();
  let valid = true;

  return valid;
};

export default checkValidation;
