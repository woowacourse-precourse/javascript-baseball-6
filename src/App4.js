const { Console } = require("@woowacourse/mission-utils");
const {
  GMAE_MESSAGE,
  BASEBALL_MESSAGE,
  END_OPTION,
  ERROR_MESSAGE,
} = require("./contants");
const { Computer } = require("./contants");

class App {
  async play() {}
}

export default App;
