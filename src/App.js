const { Console, Random } = require("@woowacourse/mission-utils");

class App {
  constructor() {       //생성자 생성 - 게임 초기화
    this.answer = [];   //(게임이 진행중인지의 상태와 랜덤으로 생성할 정답을 담을 인스턴스 멤버들을 초기화)
    this.isGameRunning = true;
  }

  async play() {
   
  }
}

export default App;
