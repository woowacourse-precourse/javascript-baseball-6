const randomNumber = () => { // 랜덤 숫자 생성
  // 랜덤 숫자 생성 로직 구현
}

class App {
  async play() {
    const retry = async() => { // 재시작 이벤트
      // 야구 게임 종료 후 재시작 로직 구현
    }

    const baseBallGame = async()  => { // 야구 게임 이벤트
      // 야구 게임 로직 구현
    }
  }
}

export default App;