import { MissionUtils } from "@woowacourse/mission-utils";

class App {
  async play() {
    let userAnswer = 1;
    MissionUtils.Console.print("숫자 야구 게임을 시작합니다.");
    while (userAnswer == 1) {}
  }
}

export default App;
