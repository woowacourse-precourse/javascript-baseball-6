const RANGE = { from: 1, to: 9 };
const SPACE = ' ';
const ERROR_HEADER = '[ERROR]';
const NUM_LENGTH = 3;

const CONSTANTS = {
  RANGE,
  SPACE,
  ERROR_HEADER,
  NUM_LENGTH,
};

export default CONSTANTS;
