/*
게임에 사용되는 상수들을 모아놓은 파일
MAX_INPUT_LENGTH, MAX_STRIKE_COUNT, MIN_RANDOM_NUM, MAX_RANDOM_NUM, RESTART_NUM, END_NUM
 */


// 사용자 입력의 최대 길이
const MAX_INPUT_LENGTH = 3;

// 스트라이크의 최대 개수
const MAX_STRIKE_COUNT = 3;

// 랜덤 숫자의 최소 값
const MIN_RANDOM_NUM = 1;

// 랜덤 숫자의 최대 값
const MAX_RANDOM_NUM = 9;


// 재시작 입력 값
const RESTART_NUM = 1;


// 종료 입력 값
const END_NUM = 2;

export {
	MAX_INPUT_LENGTH,
	MAX_STRIKE_COUNT,
	MIN_RANDOM_NUM,
	MAX_RANDOM_NUM,
	RESTART_NUM,
	END_NUM,
};