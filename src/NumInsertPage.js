import { MissionUtils } from "@woowacourse/mission-utils";

const NumInsertPage = () => {
  const user = await Console.readLineAsync('숫자를 입력해주세요.'); 

  return(
    
  );
}

export default NumInsertPage;